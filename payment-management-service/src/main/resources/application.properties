# Spring
spring.application.name=PaymentManagementService
server.port=4353
logging.level.com.fms.pms=DEBUG

# Datasource
spring.data.mongodb.uri=mongodb://localhost:27017
spring.data.mongodb.database=${MONGO_DATABASE:pms}

# AWS
aws.region=ap-south-1
aws.sqs.pms-queue-in.name=fms-sandbox-pms-queue-in
aws.sns.pms-topic-out.arn=arn:aws:sns:ap-south-1:516613527714:fms-sandbox-pms-topic-out

# Customize Swagger Path
springdoc.swagger-ui.path=/swagger-ui
# Sort the API paths in order of their HTTP methods
springdoc.swagger-ui.operationsSorter=method